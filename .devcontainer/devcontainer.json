{
    "name": "Test dotnet sample",
    "dockerFile": "Dockerfile",
    "appPort": 5000,
    "customizations": {
        "vscode": {
            "extensions": ["ms-dotnettools.csdevkit", "ms-azuretools.vscode-docker", "mhutchie.git-graph"],
            "settings": {
                "terminal.integrated.shellIntegration.enabled": true
            }
        }
    }
}